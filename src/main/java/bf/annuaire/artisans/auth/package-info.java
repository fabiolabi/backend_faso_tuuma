/**
 * Feature <strong>auth</strong> : User, RefreshToken, login / refresh / reset password (JWT
 * stateless par header). À implémenter — vide dans le socle initial.
 */
package bf.annuaire.artisans.auth;
